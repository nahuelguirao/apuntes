const numbers = [10, 35, 55, 70, 100, 4, 50, 42]

//Revierte el orden de los elementos (muta los valores del array original)
numbers.reverse()

//Genera un nuevo array sin modificar el original (ECMASCRIPT 2023)
const new_array = numbers.toReversed()