//Por convención es loading.tsx

export default function Loading() {
  return <h1>CARGANDO....</h1>;
}

//Funcionan igual que el not-found y demas, se puede configurar un loading especifico por sección
