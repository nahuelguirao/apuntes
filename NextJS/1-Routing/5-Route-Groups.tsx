//Para agrupar rutas en una carpeta se utiliza (nombreDeRuta)
//para agruparlas y que no haya que utilizar el nombre dentro de la ruta

// ejemplo =>

// (auth) => que tenga dentro 3 rutas, login, register y forgot-password
