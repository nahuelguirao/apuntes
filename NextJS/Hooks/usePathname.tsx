// import { usePathname } from 'next/navigation';

//UsePathname se utiliza para obtener la ruta actual en la que estamos

//IMPORTANTE! Se necesita usar en client components por lo que acordarse de agregar "use client"
